# WDEP コーチングアプリ 仕様書

**バージョン**: 1.2.1  
**最終更新**: 2026-05-05  
**本番 URL**: https://wdep-app.vercel.app  
**リポジトリ**: https://github.com/ponpoon5/wdep-app  
**開発者**: tatsu（ponpoon5）

---

## 目次

1. [アプリ概要・理論的背景](#1-アプリ概要理論的背景)
2. [技術スタック](#2-技術スタック)
3. [ページ・ルート構成](#3-ページルート構成)
4. [機能詳細](#4-機能詳細)
5. [質問一覧 Q1〜Q21](#5-質問一覧-q1q21)
6. [SAMIC3 チェックウィザード](#6-samic3-チェックウィザード)
7. [データモデル](#7-データモデル)
8. [localStorage キー一覧](#8-localstorage-キー一覧)
9. [状態管理（Zustand）](#9-状態管理zustand)
10. [API ルート仕様](#10-api-ルート仕様)
11. [AI プロンプト設計](#11-ai-プロンプト設計)
12. [API コスト計算](#12-api-コスト計算)
13. [CSV エクスポート / インポート仕様](#13-csv-エクスポート--インポート仕様)
14. [Markdown エクスポート仕様](#14-markdown-エクスポート仕様)
15. [モバイル対応](#15-モバイル対応)
16. [セキュリティ](#16-セキュリティ)
17. [環境変数](#17-環境変数)
18. [ディレクトリ構成](#18-ディレクトリ構成)
19. [デプロイフロー](#19-デプロイフロー)
20. [変更履歴](#20-変更履歴)

---

## 1. アプリ概要・理論的背景

### 1.1 アプリ概要

選択理論・リアリティセラピーに基づく **WDEP フレームワーク**を使い、ユーザーが自分の本当の欲求を言語化し、具体的な行動計画を立てるためのセルフコーチング Web アプリ。AI（Claude / Gemini）との対話を通じて内省を深め、毎日のコミットメント記録で変化を継続的に追跡できる。

### 1.2 選択理論・リアリティセラピーとは

**選択理論**（ウィリアム・グラッサー提唱）の核心は「すべての行動は自分の内側から動機づけられた選択である」こと。外部の刺激や他者が自分を変えるのではなく、自分が自分の行動を選択する。

**リアリティセラピー**はその理論を応用した心理療法で、現在の行動に焦点を当て、欲求を満たすより良い選択を自分で見つけることを支援する。

**5つの基本的欲求**（選択理論）:

| 欲求 | 内容 |
|---|---|
| 生存・健康 | 安全・健康・食・睡眠など生命維持の欲求 |
| 愛と所属 | つながり・愛・帰属意識の欲求 |
| 力・自己価値 | 有能感・達成・承認の欲求 |
| 自由 | 自律・選択の自由・独立の欲求 |
| 楽しみ | 遊び・笑い・学び・好奇心の欲求 |

**上質世界**とは、各個人が欲求を満たす理想の人・物・状況・信条が格納された脳内のイメージであり、WDEP の W（Wants）フェーズで明確化する対象。

### 1.3 WDEP フレームワーク

| フェーズ | 英語 | 問いの核心 | 担当問 |
|---|---|---|---|
| W | Wants | 本当に何を求めているか？ | Q1〜Q5 |
| D | Doing | 今実際に何をしているか？ | Q6〜Q9 |
| E | Evaluation | その行動は欲求達成に役立っているか？ | Q10〜Q16 |
| P | Plan | どんな計画を立てるか？ | Q17〜Q21 |

### 1.4 SAMIC3（効果的な計画の基準）

リアリティセラピーにおける効果的な計画の7基準。P フェーズの各問にセルフチェックウィザードとして組み込まれている。

| 基準 | 英語 | 問い |
|---|---|---|
| S | Simple | シンプルで一文で説明できるか |
| A | Attainable | 今の自分に達成可能か |
| M | Measurable | 数値や具体的基準で測定できるか |
| I | Immediate | 今日・明日からすぐ始められるか |
| C¹ | Controlled | 自分一人の意思で実行できるか |
| C² | Committed | 必ず実行するという確信があるか |
| C³ | Consistent | 定期的に一貫して続けられるか |

---

## 2. 技術スタック

| カテゴリ | 技術 | バージョン |
|---|---|---|
| フレームワーク | Next.js（App Router） | 16.2.4 |
| 言語 | TypeScript | 5 |
| スタイリング | Tailwind CSS | v4 |
| 状態管理 | Zustand | v5 |
| AI SDK（Claude） | @anthropic-ai/sdk | ^0.90.0 |
| AI SDK（Gemini） | @google/generative-ai | ^0.24.1 |
| アイコン | lucide-react | v1.8 |
| ホスティング | Vercel | — |
| CI/CD | GitHub → Vercel 自動デプロイ | — |
| データ永続化 | localStorage（クライアント） | — |
| バンドラー | Turbopack（Next.js 組み込み） | — |

### 2.1 AI モデル

| 用途 | Claude | Gemini |
|---|---|---|
| セッション中チャット | claude-sonnet-4-6 | gemini-2.0-flash |
| セッション完了アドバイス | claude-sonnet-4-6（maxTokens: 4096） | gemini-2.0-flash |
| SAMIC3 AI 評価 | claude-sonnet-4-6（maxTokens: 600） | — |

---

## 3. ページ・ルート構成

```
/               ダッシュボード（ホーム）
/session        WDEP セッション（Q1〜Q21 + 完了サマリー）
/needs          5つの基本的欲求アセスメント
/api/chat       AI チャット API（POST・ストリーミング）
```

### 3.1 画面遷移

```
ダッシュボード (/)
  ├─ [新しいWDEPサイクルを始める] ─→ /session（新規セッション）
  ├─ [セッションカード 継続] ─→ /session（過去セッション再開）
  └─ [5つの基本的欲求チェック] ─→ /needs

/session
  ├─ Q1〜Q21 回答
  │    └─ [完了する] ─→ セッションサマリー（同ページ内）
  └─ [ホーム] ─→ /

/needs
  └─ [WDEPサイクルを始める →] ─→ /
```

---

## 4. 機能詳細

### 4.1 ダッシュボード（`/`）

#### 毎日のコミットメント記録
- 1〜10 のボタン（`grid grid-cols-10 h-10`）で今日の本気度を記録
- 今日すでに記録済みの場合、該当ボタンを紫で強調表示
- 過去 30 日間の折れ線グラフを SVG で表示（2件以上から描画）

#### WDEP サイクル管理
- **新しいサイクル開始**: 新規セッションを作成して `/session` へ遷移
- **5つの基本的欲求チェック**: `/needs` へ遷移するボタン
- **過去のサイクル一覧**: セッションカード（日時・進捗率・コミットスコア）
  - 「続ける」: 最後に回答した問から再開
  - 「削除」: セッションを削除

#### API 使用料金表示
- Claude / Gemini 別のトークン数とコスト（¥換算）を表示
- 「リセット」ボタンで使用量ログをクリア
- `totalCost > 0` の場合のみ表示

#### CSV エクスポート / インポート
- **エクスポート**: `wdep-export-YYYY-MM-DD.csv` をダウンロード
- **インポート**: ファイル選択ダイアログ → セッション追加・API 料金合算 → 結果モーダル表示
  - 結果モーダル: 追加件数・スキップ件数・モデル別追加料金・合計追加料金

---

### 4.2 WDEP セッション（`/session`）

#### ヘッダー（スティッキー）
- **ホームボタン**: セッションをクリアして `/` へ戻る
- **アラームセッター**: 任意の間隔でアラームを設定（有用性の自問を促す）
- **進捗バー**: 回答数 / 21 をフェーズカラーで表示
- **フェーズタブ**: W / D / E / P タブ、回答済み問にはチェックマーク

#### 問ごとのレイアウト（スクロールエリア）

```
[QuestionCard]
  フェーズバッジ + 問番号
  analysis（この問の目的・灰色小文字）
  質問文
  [ヒント] 折りたたみ

[AnswerInput]
  自由記述テキストエリア

[SAMICChecker] ← P フェーズ（Q17〜Q21）のみ表示
  SAMIC3 セルフチェック + AI 評価

[AIChat]
  折りたたみパネル
  Claude / Gemini 切り替えボタン
  チャット履歴（ストリーミング）
  最後のメッセージコスト表示

[前へ / 次へ ナビゲーション]
```

#### 特殊ロジック
- **Q15 コミットメントスコア自動抽出**: 回答テキストから正規表現 `/\b([1-9]|10)\b/` で数値を検出し `commitmentScore` に保存

#### セッションサマリー画面（`completed: true` 時）
- W / D / E / P フェーズ別に全回答を折りたたみ表示
- **AI アドバイス生成**: Claude / Gemini 選択、ストリーミング（maxTokens: 4096）
- **Markdown エクスポート**: 全回答 + AI アドバイスを `.md` ファイルとしてダウンロード
- **新しいサイクルを始める**: セッションをクリアして新規開始

---

### 4.3 5つの基本的欲求アセスメント（`/needs`）

選択理論の 5 基本的欲求について欲求の強さと充足度をスライダーで入力し、ギャップを可視化する。

#### 欲求定義

| キー | 名称 | カラー | 短縮ラベル |
|---|---|---|---|
| `love` | 愛と所属 | #f43f5e（ローズ） | 愛 |
| `power` | 力・自己価値 | #f59e0b（アンバー） | 力 |
| `freedom` | 自由 | #3b82f6（ブルー） | 自 |
| `fun` | 楽しみ | #10b981（エメラルド） | 楽 |
| `survival` | 生存・健康 | #8b5cf6（バイオレット） | 健 |

#### 入力 UI

各欲求について以下の 2 スライダー（0〜10, step 1）を表示:

- **欲求の強さ**（Need Strength）: 欲求がどれだけ強いか
- **現在の充足度**（Need Satisfaction）: 今どれだけ満たされているか

スライダー変化と同時にチャートがリアルタイム更新される。

#### 質問文一覧

| 欲求 | 欲求の強さ | 現在の充足度 |
|---|---|---|
| 愛と所属 | 他の人と協力して何かをしたり、長期的な友人関係や人間関係を築きたいという思いはどのくらい強いですか？ | 現在、周囲の人から必要とされ、認められているとどの程度感じていますか？ |
| 力・自己価値 | 有能感を得たり、リーダーシップを発揮して、自分自身の能力を生かす機会を求める気持ちはどのくらい強いですか？ | 現在の生活環境において、自分の成果や存在価値をどの程度実感できていますか？ |
| 自由 | 制限を受けず、自律して独立した行動をとりたい、または自発的・柔軟に行動したいという思いはどのくらい強いですか？ | 現在の生活環境において、十分な自由や自分の裁量権があると感じている度合いはどのくらいですか？ |
| 楽しみ | 義務と遊びのバランスを取り、生活の中で楽しい経験をしたいという欲求はどのくらい強いですか？ | 現在、心から楽しいと思える時間や経験をどの程度得られていますか？ |
| 生存・健康 | 安全な環境で暮らし、心と身体の健康・セルフケア・休息を大切にしたいという気持ちはどのくらい強いですか？ | 現在の環境は、心身の健康をサポートするのに十分安全で満たされていると感じますか？ |

#### 可視化

**レーダーチャート（SVG）**
- viewBox: `0 0 200 220`、中心(100, 100)、半径 72
- 5軸の正五角形グリッド（2/4/6/8/10 の同心五角形）
- 欲求の強さ: 紫ポリゴン（`rgba(139,92,246,0.15)` + `#8b5cf6` ストローク）
- 現在の充足度: 緑ポリゴン（`rgba(16,185,129,0.15)` + `#10b981` ストローク）
- 軸ラベル: 短縮1文字（各欲求色）
- 軸角度: [-90°, -18°, 54°, 126°, 198°]（頂点から時計回り）

**ギャップ棒グラフ（SVG）**
- 横棒グラフ、各欲求のギャップ（強さ − 充足度、最小 0）を表示
- バー色は各欲求のカラー

**上位 3 ギャップ表示**
- ギャップ降順でソート、正値のみ最大 3 件表示
- 順位バッジ + 欲求名 + ギャップ数値

**自動フィードバック文**
- 最大ギャップの欲求に応じたコメント（来週のスモールステップへ誘導）
- ギャップが 0 の場合は非表示

#### スモールステップ入力
- フィードバック文の下にテキストエリア
- 「来週のスモールステップを書いてみましょう」というラベル

#### 保存・履歴
- 「結果を保存する」ボタン → localStorage に保存（保存済みで無効化）
- 「履歴」ボタン → 過去アセスメント一覧（削除ボタン付き）
- 履歴カード: 実施日・各欲求の充足度 / 強さ / ギャップ / 行動計画

#### WDEP との接続
- 「WDEPサイクルを始める →」リンクで `/` へ誘導

#### 免責注記
「このチェックは、自己理解と行動計画づくりを助けるためのものです。医学的・心理学的な診断を行うものではありません。」

---

## 5. 質問一覧 Q1〜Q21

### W フェーズ — Wants（欲求・ウォンツの明確化）

#### Q1 — このWDEPエクササイズに取り組む目的は何ですか？このワークを通じて、最終的に何を達成したいですか？

**目的**: WDEPエクササイズ自体の目的を先に明確にするメタ認知の問い

**ヒント**: WDEPを終えたとき何を手に入れていたいかを先に言語化する。例：「漠然とした焦りを解消し、毎日が充実していると感じる状態」。目的が曖昧なまま進むと後の回答も表面的になる。「自由になりたい」なら→「1日何時間の自由？どんな自由？」まで掘り下げてみよう。

**AIコーチ指示**: ユーザーのWDEPへの取り組み目的を明確化する。目的が漠然としている場合は「このワークが終わったとき、どんな状態になっていたいですか？」と問いかけ、具体的なゴールイメージを引き出す。

---

#### Q2 — 人生を通して、物ではなく「体験・状態・感覚」として本当に欲しいものは何ですか？それ以外を犠牲にしてもいいと思えるものは何ですか？

**目的**: 本当の欲求の発見。物質ではなく「体験・状態・感覚」まで掘り下げる

**ヒント**: 「お金が欲しい」→「そのお金で何を得たいか？」→「時間？自由？充実感？承認？」と深掘りする。チェック基準：手に入れた後もさらに欲しくなるか？ブランド品など「手に入れたら冷める」ものは本当の欲求ではない。最終的に多くの人が求めるのは「充実した時間」。

**AIコーチ指示**: 表面的な答え（お金・地位など）が出た場合は「それを手に入れたら何が変わりますか？」と連続して深掘りする。「手に入れたら冷めるか、さらに欲しくなるか」という基準も使う。

---

#### Q3 — 今抱えている問題がすべて解決したら、真っ先に何をしたいですか？

**目的**: 問題がマスキングしている「真の欲求」を明らかにする

**ヒント**: 現在の悩みが本当の欲求を覆い隠している。問題を解決してから夢を叶えようとするのは誤り——夢への行動と問題解決は別の軸。問題が解消しなくても「全部解決したら真っ先にしたいこと」に今すぐ取り組み始めることが大切。

**AIコーチ指示**: ユーザーが問題そのものを答えた場合は「その問題が消えた瞬間、最初に何をしますか？」と問い直す。「問題を解決してから欲求を満たそう」という考えに対して「夢への行動と問題解決は別の軸」というフレームを優しく伝える。

---

#### Q4 — あなたにとって「質が高い人生」とはどのような状態ですか？充実していると感じる瞬間を具体的に描写してください。

**目的**: 自分独自の「充実している」状態を定義する（選択理論の「上質世界」の明確化）

**ヒント**: 他人にとっての高品質が自分にとっての高品質とは限らない。「充実している」と感じる具体的な瞬間・状態を言語化する。これが明確になれば「それ以外は不要」という判断軸ができる。お金・出世などの手段ではなく、感覚として捉えよう。

**AIコーチ指示**: 抽象的な答えには「それはどんな場面ですか？誰といますか？何をしていますか？どんな感覚ですか？」と五感に訴える質問で具体化する。

---

#### Q5 — 大切な人（家族・友人・パートナーなど）から、あなたはどうあってほしいと望まれていると思いますか？

**目的**: 自分の欲求と他者の欲求を調整し、独善を防ぐ視点を得る

**ヒント**: 自分の欲求だけに集中しすぎると独善的になる。大切な人が自分に何を望んでいるかを把握する——直接聞くことも有効。自分の欲求ばかり→独善、相手に合わせすぎ→自己犠牲。両者のバランスが総合的な幸福につながる。

**AIコーチ指示**: 「わからない」と答えた場合は「直接聞いてみたことはありますか？」と問いかける。自分の欲求と他者の欲求が衝突している場合は「両者が共存できる形はありますか？」という視点を提供する。

---

### D フェーズ — Doing（現在の行動・感情の把握）

#### Q6 — 今、自分の欲求（Wで見つけたもの）を満たすために、実際に何をしていますか？行動・思考・感情の面から見てください。

**目的**: 欲求に向けた現在の行動を行動・思考・感情の3面から棚卸しする

**ヒント**: 多くの人は「本当にしたいこと」に対して何も行動できていない現実に直面する。「目先の小さな欲求への対応（スマホ・お菓子・ダラダラ）で一日が終わっていないか」を正直に問い直してみよう。行動・思考・感情の3面から棚卸しすることが大切。

**AIコーチ指示**: 行動が全くない場合でも批判せず「なぜ行動に移せていないと思いますか？」と原因探索を優しく促す。「忙しい」という答えには「その忙しさは本当にしたいことに使われていますか？」と問い返す。

---

#### Q7 — その行動をしているとき、どんなことを考えていますか？効率や生産性を意識していますか？

**目的**: 欲求に従っている状態かどうかを「効率を考えるか否か」で診断する

**ヒント**: 本当にやりたいことをしているとき、人は効率を考えない——没頭している。効率を考え始めたとき、それは「本心ではないことをこなしているサイン」かもしれない。

**AIコーチ指示**: 効率を考えていると答えた場合は「それは本当にやりたいことですか？それとも『こなして』いますか？」と問いかける。

---

#### Q8 — その行動をしているとき、どのような感情・感覚を感じますか？フロー状態や充実感はありますか？

**目的**: 欲求充足時の感情サインを把握し、再現の手がかりにする

**ヒント**: 欲求に従った行動をしているときの感情（充実感・フロー・リラックス）を記録しておく。「やらなければ」ではなく「やりたくてやっている」感覚があるかを確認しよう。

**AIコーチ指示**: ネガティブな感情（義務感・プレッシャー・焦り）が強い場合は「どんな状態でその行動ができていたら、もっと楽しめると思いますか？」と問いかける。

---

#### Q9 — 今している行動は、長期的な健康（身体・精神）にどのような影響を与えていますか？

**目的**: 健康のメタ認知——大きな欲求で小さな誘惑を自然に排除する

**ヒント**: 健康は悪しき短期的欲求（過食・過飲・睡眠不足等）に支配されやすい領域。「健康でいたい」という大きな欲求を持つことで、それに反する小さな誘惑が自然に排除される。10年後の自分が今の習慣を見たとき、どう感じるかをイメージしてみよう。

**AIコーチ指示**: 「10年後の自分は今の行動を見てどう思うと思いますか？」と問いかける。「健康でいることがあなたの欲求達成にどう関係しますか？」と健康を欲求と結びつける。

---

### E フェーズ — Evaluation（自己評価・内省）

#### Q10 — 今している行動は、あなたが望むものを手に入れるのに役立っていますか？（有用性の評価）

**目的**: 最も基本の評価軸——行動と欲求のつながりを問う

**ヒント**: 実践法：15〜30分ごとにアラームを設定し、「今していることは、望むものの獲得に役立っていますか？」と自問する。1〜2週間続けると、本当にしたい行動ができていない現実が数字でわかる。アラーム機能（本アプリ搭載）を活用しよう。

**AIコーチ指示**: 「1〜10点でつけるとしたら？」「その点数を上げるには何が必要ですか？」と問いかける。

---

#### Q11 — 今の行動は、自分が目指す理想の人生・なりたい自分像に近づけていますか？（長期的方向性）

**目的**: 短期の成果と長期の方向性が一致しているかを確認する

**ヒント**: 今の行動・選択が「なりたい自分・生きたい人生」から遠ざけていると感じたら方向を修正するサイン。たとえ短期的に成功していても、長期的に望む方向に合わなければ構造転換を検討することが必要。

**AIコーチ指示**: 「5年後の理想の自分は今の行動を続けていますか？」「続けていない場合、何が違いますか？」と未来の視点から評価を促す。

---

#### Q12 — 自分が望んでいることは実現可能だと思いますか？（現実性の評価）

**目的**: 正しい順序で考える——欲求を先に、現実への橋渡しは後で

**ヒント**: 誤った順序：現実から考える→いつも通りの選択しかできない。正しい順序：欲求を考え抜く→その後で現実的な方法を探す。「現実的に考えろ」は多くの場合「現状維持をしろ」と同義。

**AIコーチ指示**: 「なぜ無理だと思うのですか？」「その前提が間違っていたとしたら？」と可能性を広げる問いかけをする。現実制約で夢を小さくさせないよう注意する。

---

#### Q13 — 「才能がない」「もう歳だ」「運で決まる」など、欲求達成の妨げになっている考え方はありますか？（非機能的思考の評価）

**目的**: 非機能的思考パターンを発見し、手放す

**ヒント**: こうした思考は欲求達成の助けになるか？なっていないなら、その思考パターンは手放す。「頑張りが必ず報われる保証はないが、頑張らなければ確率はゼロ」。

**AIコーチ指示**: 「その考えは欲求達成に役立っていますか？」「もしその思い込みがなければ、何ができると思いますか？」と問いかける。「その思考は手放せそうですか？」という選択の問いかけで終える。

---

#### Q14 — 欲求を満たすために、どれだけの努力をする覚悟がありますか？（覚悟の評価）

**目的**: 覚悟は意志力ではなく欲求の深さから自然に生まれる

**ヒント**: 「何でもやる」と思えるレベルまで覚悟が持てないなら、欲求の掘り下げが不足しているサイン。覚悟は精神論ではなく、欲求を深く掘り下げることで自然に生まれるもの。覚悟が薄いと感じたら Q2 に戻って欲求をさらに掘り下げよう。

**AIコーチ指示**: 覚悟が薄い場合は「本当に手に入れたいものに気づいていますか？」「それを手に入れた自分を想像したとき、どんな感覚がしますか？」と欲求のリアリティを高める問いかけをする。

---

#### Q15 — 欲求達成への現在のコミットメントを1〜10点で評価すると何点ですか？その理由は？

**目的**: 本気度を数値化して定期的に記録する自己採点

**ヒント**: スコアが低ければ原因を探る（欲求が曖昧？環境が邪魔？）。毎日採点して記録することで自己認識が変わる。ホーム画面でも毎日スコアを記録できる——継続が大切。

> **実装メモ**: この回答から正規表現 `/\b([1-9]|10)\b/` で数値を自動抽出し、`commitmentScore` フィールドに保存する。ホーム画面のコミットメントトラッカーと連動。

**AIコーチ指示**: スコアの理由を深掘りし、低い場合は「スコアを1点上げるために、今日できることは何ですか？」と小さな改善を促す。

---

#### Q16 — 欲求達成のために立てている計画は、役に立っていますか？（計画の評価）

**目的**: 計画が機能しているかを客観視し、設計の問題として捉え直す

**ヒント**: 計画が実行できていないなら、欲求を抑えるのではなく計画自体を見直す。計画の問題を「自分の意志力不足」と解釈するのは誤り——設計の問題として捉え直そう。

**AIコーチ指示**: 計画が機能していない場合は「計画のどの部分が難しいですか？」「どう変えれば続けられますか？」と改善点を探るよう促す。

---

### P フェーズ — Plan（具体的な行動計画）

> P フェーズ（Q17〜Q21）では、回答入力後に **SAMIC3 チェックウィザード** が表示される。詳細は [§6](#6-samic3-チェックウィザード) を参照。

#### Q17 — 欲求に向かうために、今と違う方法・アプローチをどんなことでも試してみる用意がありますか？複数の代替案を考えてみてください。

**目的**: 方法の柔軟性を持ち、複数の手段をストックする

**ヒント**: 同じやり方に固執すると飽きたり詰まったりしたときに止まる。同じ欲求を満たす複数の方法をストックしておく。「無計画でうまくいく人は欲望に忠実に生きている人」——行動しながら方法を更新していく発想でよい。

**AIコーチ指示**: 「今のやり方以外に、同じ欲求を満たせる方法を3つ挙げてみてください」と創造的な発想を促す。

---

#### Q18 — 具体的に何をするか、明確に言葉にできますか？「この計画通りに実行すれば、最高の1日だった」と思えますか？

**目的**: 迷いをなくすための具体化——効率より「続けられる確信」を優先する

**ヒント**: 計画の目的は効率化ではなく「迷いをなくし、欲求に向かって進む確信を持つこと」。「最高の1日だった」と思えるかどうかが判断基準。効率的には午後が良くても「朝の方が気持ちよく続けられる」なら朝でよい。

**AIコーチ指示**: 計画が漠然としている場合は「具体的に明日の朝、最初の15分で何をしますか？」と時間・場所・行動を具体化する。

---

#### Q19 — 今立てている計画は、現実的に達成可能ですか？無理な場合、どう工夫できますか？

**目的**: 理想と現実のギャップを埋める——欲求は下げず、満たし方を工夫する

**ヒント**: 達成困難なら欲求を下げるのではなく、満たし方の工夫や別のアプローチを考える。最初の一歩は「今の自分が確実にできる最小の行動」に設定する。

**AIコーチ指示**: 「最初の1週間だけで考えると、何ができますか？」と小さく始める視点を提供する。「最初の一歩を最小化する」というアプローチを勧める。

---

#### Q20 — 欲求を達成したとき、どうやって「達成した」と判断しますか？具体的な達成指標を決めてください。

**目的**: ゴールポストを動かさないための達成指標を事前に設定する

**ヒント**: 指標がないと「いつまでも頑張り続ける」「達成しても満足できない」状態に陥る。達成後に新たな欲求・指標が生まれたら、WDEP の次のサイクルで Wants を更新しよう。

**AIコーチ指示**: 「達成した日の朝、あなたはどんな気持ちで目が覚めますか？」「周囲の人は何と言っていますか？」と具体化を促す。

---

#### Q21 — 今すぐ実行に移せる「最初の一歩」は何ですか？このワークが終わったあと、最初にする行動を決めてください。

**目的**: 先延ばしを防ぐ即行動の設計——計画を立てたその場で一歩を踏む

**ヒント**: 大きすぎる最初の一歩は先延ばしの原因になる——「確実にできる最初の行動」に分解しよう。「今夜寝る前にできること」くらいの小さな一歩でいい。一度着手すれば完了させたいという心理が働き行動が続きやすくなる。

**AIコーチ指示**: 「今日の夜、寝る前にできることは何ですか？」「明日の朝一番にできることは？」と最も小さくすぐできる行動を引き出す。完璧な準備より今すぐの一歩を促す。

---

## 6. SAMIC3 チェックウィザード

### 6.1 概要

P フェーズ（Q17〜Q21）の各問において、回答テキストエリアと AI チャットパネルの間に表示される折りたたみ式の自己評価ウィジェット。

ユーザーが書いた行動計画が SAMIC3 の 7 基準を満たしているかをセルフチェックし、不足している基準に対してヒントを提示する。AI による評価も呼び出せる。

### 6.2 表示条件

`question.phase === 'P'`（Q17〜Q21）の場合のみ表示。

### 6.3 UI 構成

```
[ヘッダー行]
  ☑ SAMIC3 チェック  [n/7 バッジ]  [▼ 開閉]

[展開時]
  説明文: 効果的な計画の7基準でセルフチェックしましょう。

  [チェックリスト × 7]
    各項目:
      [チェックボックス]  [S/A/M/I/C¹/C²/C³ バッジ]
      ラベル（英語 — 日本語）
      質問文
      ヒント（未チェック時のみ表示、amber 色）

  [スコアサマリー]
    全クリア時: 緑のバナー「全基準クリア！この計画は実行力が高いです」
    部分クリア時: 「未チェック: ○○・△△ を見直すと計画の質が上がります。」

  [AI 評価エリア]
    [AIにSAMIC3評価をしてもらう] ボタン
    ※ 回答未入力時はボタン無効 + 注意文表示
    AI 結果: ストリーミングで表示
```

### 6.4 SAMIC3 項目定義

| キー | 英語 | 日本語 | 自問チェック | 未チェック時ヒント |
|---|---|---|---|---|
| S | Simple | シンプル | この計画は一文で説明できるくらいシンプルで明確ですか？ | 複雑すぎる計画は実行されにくい。まず最小の行動に絞ってみましょう。 |
| A | Attainable | 達成可能 | 今のあなたに達成可能な難易度ですか？高すぎず低すぎませんか？ | 「少し頑張れば届く」レベルが理想。高すぎると挫折し、低すぎると成長がない。 |
| M | Measurable | 測定可能 | 達成できたかどうかを数字や具体的な基準で確認できますか？ | 「毎日30分」「週3回」のように数値化することで達成を客観的に確認できる。 |
| I | Immediate | 即時的 | 「いつか」ではなく今日・明日からすぐに始められますか？ | 開始を先延ばしにすると実行率が大幅に低下する。今日できる最小の一歩を設定しよう。 |
| C¹ | Controlled | 自己管理 | 他者や環境に左右されず、自分一人の意思で実行できますか？ | 他者の協力が前提の計画は不安定。まず自分だけでできる行動から始めよう。 |
| C² | Committed | コミット済み | 必ず実行するという確信がありますか？声に出して「やる」と言えますか？ | コミットメントを言語化（宣言）すると実行率が上がる。誰かに伝えるとさらに効果的。 |
| C³ | Consistent | 一貫性 | 毎日・毎週など、定期的に一貫して続けられる行動ですか？ | 「毎○曜日の○時」という習慣に落とし込むほうが継続しやすい。 |

### 6.5 スコアバッジの色

| 状態 | 色 |
|---|---|
| 0/7（未着手） | グレー（`bg-gray-100 text-gray-500`） |
| 1〜6/7（進行中） | エメラルド（`bg-emerald-200 text-emerald-700`） |
| 7/7（全クリア） | 緑（`bg-emerald-500 text-white`） |

### 6.6 AI 評価プロンプト

```
あなたはリアリティセラピーの専門コーチです。ユーザーが立てた行動計画をSAMIC3の
7つの基準で評価してください。

SAMIC3基準:
- S（Simple）: シンプルで一文で説明できるか
- A（Attainable）: 今の自分に達成可能か
- M（Measurable）: 数字や具体的基準で測定できるか
- I（Immediate）: 今日・明日からすぐ始められるか
- C¹（Controlled）: 他者に左右されず自分一人で実行できるか
- C²（Committed）: 必ず実行するという確信があるか
- C³（Consistent）: 定期的に一貫して続けられるか

各基準について「○（満たしている）」「△（改善の余地あり）」「×（満たしていない）」で評価し、
△や×の項目には具体的な改善提案を1つ提示してください。温かく励ましのトーンで、
250字以内で回答してください。日本語で。
```

- モデル: claude-sonnet-4-6
- maxTokens: 600
- ストリーミング表示（`/api/chat` エンドポイントを使用）

### 6.7 チェック状態の保存

- `WDEPSession.samicChecks?: Record<number, boolean[]>` に格納
- キー: 質問ID（17〜21）
- 値: 長さ 7 の boolean 配列（SAMIC3 の順序と対応）
- 問を移動しても保持される（localStorage に即時保存）

---

## 7. データモデル

### 7.1 WDEPSession

```typescript
interface WDEPSession {
  id: string;                                    // UUID（Math.random() ベース）
  createdAt: string;                             // ISO 8601
  updatedAt: string;                             // ISO 8601
  answers: Record<number, string>;               // questionId → 回答テキスト
  chatHistories: Record<number, ChatMessage[]>;  // questionId → AI チャット履歴
  samicChecks?: Record<number, boolean[]>;       // questionId → 7 booleans（SAMIC3）
  commitmentScore?: number;                      // 1〜10（Q15 から自動抽出）
  completed: boolean;
}
```

### 7.2 ChatMessage

```typescript
interface ChatMessage {
  role: 'user' | 'assistant';
  content: string;
  timestamp: string;  // ISO 8601
}
```

### 7.3 DailyScore

```typescript
interface DailyScore {
  date: string;   // YYYY-MM-DD
  score: number;  // 1〜10
}
```

### 7.4 Question

```typescript
interface Question {
  id: number;           // 1〜21
  phase: 'W' | 'D' | 'E' | 'P';
  text: string;         // 質問文
  analysis: string;     // この質問が問うことの説明（カード上部に表示）
  hint: string;         // ヒントテキスト（折りたたみ）
  aiPrompt: string;     // AI コーチへの追加指示
}
```

### 7.5 NeedScore / NeedScores

```typescript
type NeedKey = 'love' | 'power' | 'freedom' | 'fun' | 'survival';

interface NeedScore {
  strength: number;     // 0〜10（欲求の強さ）
  satisfaction: number; // 0〜10（現在の充足度）
}

type NeedScores = Record<NeedKey, NeedScore>;
```

### 7.6 NeedAssessmentRecord

```typescript
interface NeedAssessmentRecord {
  id: string;           // UUID
  date: string;         // YYYY-MM-DD
  scores: NeedScores;
  smallStep: string;    // スモールステップ行動計画
  topGapNeed: NeedKey;  // 最大ギャップの欲求キー
  sessionId?: string;   // WDEPセッションとの紐付け（任意・未使用）
}
```

### 7.7 UsageEntry（内部型）

```typescript
interface UsageEntry {
  model: 'claude' | 'gemini';
  inputTokens: number;
  outputTokens: number;
}
```

---

## 8. localStorage キー一覧

| キー | 型 | 内容 | ソート順 |
|---|---|---|---|
| `wdep_sessions` | `WDEPSession[]` | セッション一覧 | 新しい順（先頭が最新） |
| `wdep_daily_scores` | `DailyScore[]` | コミットメントスコア | 日付昇順 |
| `wdep_api_usage` | `UsageEntry[]` | API 使用ログ | 追記順 |
| `wdep_needs_assessments` | `NeedAssessmentRecord[]` | 欲求アセスメント | 新しい順（先頭が最新） |

---

## 9. 状態管理（Zustand）

`store/sessionStore.ts` にセッション操作を集約。すべての書き込みは即座に localStorage へ反映される。

```typescript
interface SessionStore {
  activeSession: WDEPSession | null;
  currentQuestionId: number;              // 1〜21

  startSession: () => void;               // 新規セッション作成・保存
  loadSession: (session) => void;         // 過去セッション再開（最後回答問へ移動）
  setAnswer: (qId, answer) => void;       // 回答保存
  addChatMessage: (qId, msg) => void;     // AI チャット履歴追加
  setSamicChecks: (qId, checks) => void;  // SAMIC3 チェック状態保存
  setCommitmentScore: (score) => void;    // コミットメントスコア設定
  completeSession: () => void;            // セッション完了（completed: true）
  goToQuestion: (id) => void;            // 問移動（保存なし）
  clearSession: () => void;              // activeSession をクリア
}
```

---

## 10. API ルート仕様

### POST `/api/chat`

AI チャット（ストリーミングレスポンス）。Claude と Gemini の両モデルに対応。

**リクエストボディ**

```typescript
{
  messages: { role: 'user' | 'assistant'; content: string }[];
  systemPrompt: string;
  model?: 'claude' | 'gemini';  // デフォルト: 'claude'
  maxTokens?: number;           // デフォルト: 1024
}
```

**レスポンス**

```
Content-Type: text/plain; charset=utf-8
Transfer-Encoding: chunked（ストリーミング）
```

- テキストチャンクをストリーミング送信
- 最後のチャンクに `\x00USAGE:{json}` マーカーを付与

**USAGE マーカーの JSON**

```typescript
{
  model: 'claude' | 'gemini';
  inputTokens: number;
  outputTokens: number;
}
```

**Claude 実装詳細**
- SDK: `@anthropic-ai/sdk` の `client.messages.stream()`
- モデル: `claude-sonnet-4-6`
- `inputTokens`: `message_start` イベントから取得
- `outputTokens`: `message_delta` イベントの `usage.output_tokens` から取得

**Gemini 実装詳細**
- SDK: `@google/generative-ai` の `chat.sendMessageStream()`
- モデル: `gemini-2.0-flash`
- `usageMetadata`: ストリーム完了後（`response.usageMetadata`）から取得
- システムプロンプトは `systemInstruction` として設定

**クライアント側の USAGE マーカー処理**

`hooks/useAIChat.ts` / `components/wdep/SessionSummary.tsx` / `components/wdep/SAMICChecker.tsx` の 3 箇所がストリームを読みながら同じパターンで処理する。

```typescript
let accumulated = '';        // 表示用本文
let usageBuffer = '';        // USAGE JSON（チャンク跨ぎを許容）
let inUsage = false;

while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  const chunk = decoder.decode(value, { stream: true });

  if (inUsage) {
    // マーカーは検出済み。以降の全チャンクを JSON バッファに積む
    usageBuffer += chunk;
    continue;
  }
  const markerIdx = chunk.indexOf('\x00USAGE:');
  if (markerIdx !== -1) {
    accumulated += chunk.slice(0, markerIdx);
    usageBuffer += chunk.slice(markerIdx + 7);
    inUsage = true;
  } else {
    accumulated += chunk;
  }
}

// done 後に一括パース（チャンク境界で USAGE JSON が分断されても安全）
if (usageBuffer) {
  try {
    const usage = JSON.parse(usageBuffer);
    recordUsage(usage);
  } catch { /* ignore */ }
}
```

**重要な実装ルール**
- USAGE マーカー（`\x00USAGE:`）以降の JSON は **必ず `done` 後にパースする**。1 回の `reader.read()` 内で完結している保証はない。
- 3 つの呼び出し箇所すべてで `recordUsage()` を呼ぶこと。SAMIC3 AI 評価分が抜けると累計料金が過小表示になる。

**入力トークンの累積（重要）**

セッション中チャットは毎ターン `[...messages, userMsg]`（過去履歴全件）を送信する。プロンプトキャッシュ未使用のため、N ターン目の入力トークンは概ね 1〜N ターン目の合計に比例して増加する。これは Claude / Gemini ともに仕様上の挙動。

---

## 11. AI プロンプト設計

### 11.1 セッション中 AI チャット（各問）

```
あなたはWDEPフレームワーク（選択理論カウンセリング）の専門コーチです。
以下の知識を背景として持ち、コーチングに活かしてください：

{WDEP_KNOWLEDGE}  ← lib/wdep-knowledge.ts の内容
---

【現在の質問】Q{id}（{phase}フェーズ）
{question.text}

【AIのガイドライン】
{question.aiPrompt}

【ユーザーの現在の回答】
{currentAnswer または '（まだ回答なし）'}

【Wantsフェーズの回答（参考）】
{Q1〜Q5 の回答サマリー または '（まだなし）'}

指示：
- 共感的で温かいトーンで接してください
- 批判・評価はしないでください
- 質問は一度に1〜2個まで
- 日本語で回答してください
- ユーザーが自分で気づけるよう、答えを言わず問いかけてください
- 短く簡潔に（200字以内を目安）
```

### 11.2 セッション完了後アドバイス

```
あなたはWDEPフレームワークの専門コーチです。
ユーザーがWDEPサイクルを完了しました。全回答を踏まえた総合的なアドバイスをしてください。

{全21問の回答}

指示：
- 回答全体を俯瞰した洞察を提供
- ユーザーの欲求・行動・評価・計画の一貫性を確認
- 具体的なアクションを提案
- 励ましとサポートを忘れずに
- 日本語、1000〜2000字程度
```

maxTokens: 4096（アドバイスは長文を許容）

### 11.3 SAMIC3 AI 評価

→ [§6.6](#66-ai-評価プロンプト) 参照

---

## 12. API コスト計算

```typescript
// lib/usage.ts
const PRICES: Record<AIModel, { input: number; output: number }> = {
  claude: { input: 3 / 1_000_000,    output: 15 / 1_000_000 },  // USD/token
  gemini: { input: 0.10 / 1_000_000, output: 0.40 / 1_000_000 }, // USD/token
};

const JPY_RATE = 155; // 1 USD = 155 JPY（固定レート）

// コスト計算
cost_usd = inputTokens * PRICES[model].input + outputTokens * PRICES[model].output

// 円換算表示
toYen(usd):
  yen = usd * 155
  if yen < 0.01: return '< ¥0.01'
  else: return `¥${yen.toFixed(2)}`
```

コストは `wdep_api_usage` に UsageEntry として追記記録され、`getUsageSummary()` でモデル別・合計を集計する。

### 12.1 表示計算とエクスポート値の関係

- `wdep_api_usage` には **`{ model, inputTokens, outputTokens }` のみ** を保存する。コストは保存しない。
- ダッシュボードの累計料金表示は、表示時に現在の `PRICES` / `JPY_RATE` を使って毎回再計算される。
- CSV エクスポートの `cost_usd` / `cost_yen` 列はエクスポート時点の価格表で算出された **スナップショット**。
- CSV インポート時は `cost_usd` 列を採用せず、**`inputTokens` / `outputTokens` のみを `recordUsage()` に渡す**（`lib/csv.ts` 参照）。表示は再計算結果になるため、価格表を変更したあとに過去の CSV をインポートすると、CSV に書かれた `cost_yen` と画面表示が一致しないことがある（仕様）。

### 12.2 トークンが記録されるイベント

| 呼び出し元 | システムプロンプト長 | maxTokens | recordUsage |
|---|---|---|---|
| `useAIChat` (各問のチャット) | 大（WDEP_KNOWLEDGE 全文 + 質問メタ） | 1024 | ✅ |
| `SessionSummary` (完了アドバイス) | 大（WDEP_KNOWLEDGE 全文 + 全21回答） | 4096 | ✅ |
| `SAMICChecker` (P フェーズ AI 評価) | 中（SAMIC3 基準説明） | 600 | ✅ |

すべて `/api/chat` 経由で課金が発生するため、3 経路すべてで `recordUsage()` が呼ばれていることを変更時に確認すること。

---

## 13. CSV エクスポート / インポート仕様

### 13.1 ファイル形式

- エンコーディング: UTF-8
- 改行: `\n`
- セル内に `,` `"` `\n` が含まれる場合は RFC 4180 形式でエスケープ（`"` → `""` でクォート）
- ファイル名: `wdep-export-YYYY-MM-DD.csv`

### 13.2 フォーマット

```
SECTION,SESSIONS
date,session_id,completed,commitment_score,answered_count,Q1,Q2,Q3,...,Q21
2026-04-01,xxxxxxxx-xxxx-4xxx-yxxx,true,8,21,"回答1の内容","回答2",...

SECTION,USAGE
model,input_tokens,output_tokens,cost_usd,cost_yen
claude,12345,4567,0.000185,¥0.29
gemini,8000,2000,0.0000016,< ¥0.01

TOTAL,,,0.000187,¥0.29
```

### 13.3 インポート動作

| 条件 | 動作 |
|---|---|
| 新規 session_id | セッションを追加（`sessionsAdded++`） |
| 既存 session_id | スキップ（`sessionsSkipped++`） |
| USAGE エントリ | `recordUsage()` で追記（累算） |
| `TOTAL,` 行 | スキップ |
| 空行 | スキップ |

インポート結果として `ImportResult` を返す:

```typescript
interface ImportResult {
  sessionsAdded: number;
  sessionsSkipped: number;
  usageAdded: { model: string; inputTokens: number; outputTokens: number; costYen: string }[];
  totalCostYen: string;  // 追加された usage の合計コスト
}
```

---

## 14. Markdown エクスポート仕様

セッションサマリー画面の「Markdownで保存」ボタンからダウンロード。

```markdown
# WDEPセッション — YYYY-MM-DD

## Wants（欲求・ウォンツ）
### Q1: 質問文
> 回答内容

### Q2: 質問文
> 回答内容

...

## Doing（現状確認）
### Q6: 質問文
> 回答内容

...

## Evaluation（評価）
...

## Plan（計画）
...

## AI アドバイス
（アドバイス生成済みの場合のみ出力）
アドバイス本文...
```

- 未回答の問は `（未回答）` と表示
- AI アドバイスセクションはアドバイス生成済みの場合のみ追加
- ファイル名: `wdep-session-YYYY-MM-DD.md`

---

## 15. モバイル対応

| 課題 | 対策 |
|---|---|
| `crypto.randomUUID` が HTTP 環境で使えない | `Math.random()` ベースの `generateId()` を独自実装 |
| SVG チャートがタッチをブロック | `width: 100%; height: auto`（`h-auto`）で overflow を防止 |
| コミットメントボタンのタップ領域が小さい | `grid grid-cols-10 h-10`（40px 高）を採用 |
| ネストした `<button>` による操作不能 | AIChat / SessionSummary のモデル切り替えを `<div>` 構造で分離 |
| LAN 外からのアクセス不可 | Vercel にデプロイ（HTTPS）して外出先からも利用可能に |
| ローカル開発中のモバイルホットリロード | `next.config.ts` の `allowedDevOrigins: ['192.168.0.231']` |

---

## 16. セキュリティ

| 項目 | 内容 |
|---|---|
| API キー管理 | Next.js API Route（サーバーサイド）でのみ参照。`.env.local` は `.gitignore` 対象。 |
| クライアントへの非公開 | `ANTHROPIC_API_KEY` / `GOOGLE_GENERATIVE_AI_API_KEY` はブラウザに送信されない |
| Vercel での管理 | Environment Variables ダッシュボードで設定。コードへの直書きなし |
| アクセス制限 | 現在は認証なし（URL を知っていれば誰でもアクセス可能）。個人利用前提。 |
| データ保存先 | すべてクライアントの localStorage。サーバーにユーザーデータは保存されない |

---

## 17. 環境変数

| 変数名 | 設定場所 | 用途 |
|---|---|---|
| `ANTHROPIC_API_KEY` | `.env.local` / Vercel | Claude API 認証 |
| `GOOGLE_GENERATIVE_AI_API_KEY` | `.env.local` / Vercel | Gemini API 認証 |

---

## 18. ディレクトリ構成

```
wdep-app/
├── app/
│   ├── page.tsx                  # ダッシュボード（/）
│   ├── session/
│   │   └── page.tsx              # WDEPセッション（/session）
│   ├── needs/
│   │   └── page.tsx              # 欲求アセスメント（/needs）
│   ├── api/
│   │   └── chat/
│   │       └── route.ts          # AI チャット API（POST /api/chat）
│   ├── layout.tsx                # ルートレイアウト
│   └── globals.css               # グローバルスタイル
│
├── components/
│   ├── dashboard/
│   │   ├── CommitmentTracker.tsx # コミットメント記録 + 折れ線グラフ
│   │   └── SessionCard.tsx       # セッション一覧カード
│   ├── wdep/
│   │   ├── QuestionCard.tsx      # 質問カード（analysis + text + hint）
│   │   ├── AnswerInput.tsx       # 回答テキストエリア
│   │   ├── AIChat.tsx            # AI チャットパネル（折りたたみ）
│   │   ├── SAMICChecker.tsx      # SAMIC3 チェックウィザード（P フェーズ）
│   │   ├── PhaseHeader.tsx       # フェーズタブナビゲーション
│   │   └── SessionSummary.tsx    # セッション完了サマリー
│   └── ui/
│       ├── Button.tsx            # 共通ボタン（variant / size 対応）
│       ├── ProgressBar.tsx       # 進捗バー
│       └── AlarmSetter.tsx       # アラーム設定
│
├── hooks/
│   └── useAIChat.ts              # AI チャットロジック（ストリーミング・USAGE解析）
│
├── lib/
│   ├── types.ts                  # 全型定義
│   ├── questions.ts              # Q1〜Q21 定義 + フェーズ定数
│   ├── storage.ts                # localStorage CRUD
│   ├── usage.ts                  # API コスト計算・記録
│   ├── csv.ts                    # CSV エクスポート / インポート
│   └── wdep-knowledge.ts         # AI 用 WDEP 知識ベース
│
├── store/
│   └── sessionStore.ts           # Zustand ストア
│
├── public/                       # 静的ファイル
├── next.config.ts                # Next.js 設定（allowedDevOrigins）
├── package.json
├── tsconfig.json
├── postcss.config.mjs
└── SPEC.md                       # 本仕様書
```

---

## 19. デプロイフロー

### 本番デプロイ

```
git add .
git commit -m "変更内容"
git push origin master
       ↓
Vercel が GitHub を自動検知
       ↓
next build（Turbopack）
       ↓
https://wdep-app.vercel.app に反映（約1〜2分）
```

### ローカル開発

```bash
npm run dev
# http://localhost:3000
# LAN: http://192.168.0.231:3000（スマホからアクセス用）
```

### 環境変数設定（初回のみ）

```bash
# .env.local を作成
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_GENERATIVE_AI_API_KEY=AIza...

# Vercel は Dashboard > Settings > Environment Variables に同じ値を設定
```

---

## 20. 変更履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| 1.0.0 | 2026-04-18 | 初回リリース。WDEP Q1〜Q21、Claude チャット、localStorage 保存 |
| 1.0.1 | 2026-04-18 | Gemini 対応（チャット・アドバイス）、モデル切り替えボタン |
| 1.0.2 | 2026-04-18 | Markdown エクスポート、AI アドバイス生成 |
| 1.0.3 | 2026-04-19 | 毎日コミットメント記録・折れ線グラフ追加 |
| 1.0.4 | 2026-04-20 | モバイル対応（`generateId`・SVG overflow・タップ領域修正） |
| 1.0.5 | 2026-04-21 | API 使用料金表示（¥換算・モデル別・リセット） |
| 1.1.0 | 2026-04-25 | CSV エクスポート / インポート機能追加 |
| 1.1.1 | 2026-04-25 | 全21問の `analysis` フィールド・詳細ヒント・aiPrompt を拡充 |
| 1.1.2 | 2026-04-25 | 5つの基本的欲求アセスメント（`/needs`）追加 |
| 1.1.3 | 2026-04-25 | Vercel デプロイ（https://wdep-app.vercel.app） |
| 1.2.0 | 2026-05-05 | SAMIC3 チェックウィザード追加（P フェーズ Q17〜Q21） |
| 1.2.1 | 2026-05-05 | 使用トークン記録のバグ修正：SAMIC3 AI 評価で `recordUsage()` 未実行を解消／USAGE マーカー JSON のチャンク跨ぎ分断を許容するパーサに変更／モデル名を `claude-sonnet-4-6` に統一／§10・§12 に入力トークン累積仕様と CSV インポート時の再計算挙動を明記 |
